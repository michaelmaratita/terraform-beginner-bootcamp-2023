provider_installations {
    filesystem_mirror {
        path = "home/gitpod/.terraform/plugins"
        include = ["local.providers/*/*"]
    }
    direct {
        exclude = ["local.providers/*/*"]
    }
}